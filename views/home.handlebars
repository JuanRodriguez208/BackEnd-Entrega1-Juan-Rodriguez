<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
</head>
<body>
  <h1>{{title}}</h1>
  <ul>
    {{#each products}}
      <li>{{this.title}} - {{this.price}}$</li>
    {{/each}}
  </ul>
  <a href="/realtimeproducts">Ver productos en tiempo real</a>
</body>
</html>
